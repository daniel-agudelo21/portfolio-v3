---
interface Props {
    title: string;
    date: string;
    universidad: string;
}

const { title, date, universidad } = Astro.props;
---

<div
    class="absolute w-3 h-3 rounded-full mt-12 -start-1.5 border border-gray-900 bg-gray-700 "
>
</div>
<h3 class="text-2xl mb-2 font-semibold">{title}</h3>
<p class="mb-4 mt-1">{universidad}</p>
<time
    datetime=""
    class="mt-10 text-sm "
    >{date}</time
>
